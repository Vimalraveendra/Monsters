version: 2

jobs:
  build:
    docker:
      - image: circleci/node:10.16.3
    steps:
      - checkout
      - run: echo "hello world"
  test:
    docker:
      - image: circleci/node:10.16.3
    steps:
      - checkout
      - run: echo "double trouble"
workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test
